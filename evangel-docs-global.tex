\usepackage{fontspec}

\setmainfont{LibertinusSerif}[
    Path = fonts/,
    Extension = .ttf,
    UprightFont = *-Regular,
    BoldFont = *-Bold,
    ItalicFont = *-Italic,
    BoldItalicFont = *-BoldItalic
]

\setsansfont{LibertinusSans}[
    Path = fonts/,
    Extension = .ttf,
    UprightFont = *-Regular,
    BoldFont = *-Bold,
    ItalicFont = *-Italic,
]

% Deescape next line if you want ragged right
% \raggedright

% If you want to let LaTeX calculate, DIV=11 is the best w/ 12pt font
% \KOMAoptions{fontsize=12pt, DIV=11}

% Alex's custom margins
\usepackage[
  top=1.25in,       % Top margin
  bottom=1.5in,     % Bottom margin (includes footer space)
  left=1.5in,       % Inner/Left margin
  right=1.5in,      % Outer/Right margin
  headsep=.42in,    % Distance from header baseline to text area
  footskip=0.58in,  % Distance from text are to page number baseline
]{geometry}

% Only use this if you're using LaTeX DIV to calculate margins
% \KOMAoptions{DIV=last} 

\usepackage{setspace}
% Set leading to 1.2
\setstretch{1.2}

% Only use this if you're using LaTeX DIV to calculate margins
% \recalctypearea

\usepackage[automark]{scrlayer-scrpage}

% Remove %s on next two lines to force headers and sans-serif elements to use the main font family
% \renewcommand{\familydefault}{\rmdefault}
% \setkomafont{disposition}{\rmfamily\bfseries}

% Set header font size to 10/12pt and remove default KOMA italics
\setkomafont{pagehead}{\fontsize{10}{12}\normalfont\selectfont}

% Header: Centered, Tracked (+100), All Caps
\makeatletter
\chead{\textls[100]{\MakeUppercase{\@title\ \textendash\ \headmark}}}
\makeatother

\pagestyle{scrheadings}

% Continuing footnote rule
\usepackage[splitrule]{footmisc}

% \deffootnote[fn num box width]{1st para indent}{subsequent para w/in fn indent}{footnote number format, and specifying it this way makes it plain text instead of superscript}
\deffootnote[1.5em]{1.5em}{1em}{\thefootnotemark.\ }

% Adds space between separate footnotes
\addtolength{\footnotesep}{0.3\baselineskip}
